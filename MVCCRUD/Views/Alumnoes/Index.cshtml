@model IEnumerable<MVCCRUD.Models.Alumno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreAlumno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMateriaNavigation.NombreMateria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProfesorNavigation.NombreProfesor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreAlumno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMateriaNavigation.NombreMateria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProfesorNavigation.NombreProfesor)
            </td>
            <td>
                <a asp-action="Edit"  class="btn btn-warning" asp-route-id="@item.IdAlumno">Edit</a> |
                <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdAlumno">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdAlumno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
